Legacy experiment configs live here.

They are kept for ablation and historical comparison only.

They are not part of the default final method:

- Fused Query Alignment
- Projected Semantic Gate Readout

Default training and evaluation should use:

- `config/train_scannet_v2_final.yaml`
- `config/eval_scannet_v2_final_readout.yaml`
