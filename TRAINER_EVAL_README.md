# 使用Trainer验证代码的评估脚本

## 简介

`eval_using_trainer.py` 直接复用训练代码中的验证逻辑 (`trainer._validate()`)，确保评估结果与训练时的验证指标完全一致。

## 使用方法

```bash
cd /home/featurize/work/mix
source /home/featurize/work/envs/mix_backup/bin/activate

python eval_using_trainer.py \
  --checkpoint checkpoints/full.4/checkpoint_epoch_36.pth \
  --config config/train_scannet_v2_full_multi_gpu.yaml
```

## 参数说明

- `--checkpoint`: 模型checkpoint文件路径（必需）
- `--config`: 训练时使用的YAML配置文件（必需）
- `--device`: 运行设备，默认 `cuda`

## 优势

1. **完全一致**: 使用与训练完全相同的验证代码
2. **简单直接**: 无需重写评估逻辑，直接调用 `trainer._validate()`
3. **结果可信**: 保证评估指标与训练时验证指标计算方式相同
4. **自动处理**: 
   - 自动处理DDP模型的 `module.` 前缀
   - 自动加载数据集和模型配置
   - 自动计算各类别IoU

## 与 eval_model_simple.py 的对比

| 特性 | eval_using_trainer.py | eval_model_simple.py |
|------|----------------------|---------------------|
| **代码来源** | 复用训练验证代码 | 参考OpenScene重写 |
| **一致性** | ✅ 100%与训练一致 | ⚠️ 需要手动对齐 |
| **复杂度** | ✅ 简单（~150行） | ⚠️ 复杂（~400行） |
| **维护性** | ✅ 训练代码改动自动同步 | ⚠️ 需要手动同步 |
| **推荐度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

## 输出示例

```
============================================================
使用Trainer验证代码进行评估
============================================================
Checkpoint: checkpoints/full.4/checkpoint_epoch_36.pth
Config: config/train_scannet_v2_full_multi_gpu.yaml
============================================================

📋 加载配置...
✓ 数据集: /home/featurize/data/scannet/scans
✓ 模型架构: MinkUNet34C

🔧 创建Trainer...
✓ Trainer创建成功

📦 加载checkpoint: checkpoints/full.4/checkpoint_epoch_36.pth
✓ Epoch: 35
✓ 训练时最佳IoU: 0.3034
✓ 训练时最佳Loss: 1.2650

============================================================
🚀 开始评估...
============================================================

评估: 100%|████████████████████| 26687/26687 [25:30<00:00, 17.45it/s]

============================================================
📊 评估结果
============================================================

验证Loss: 1.2650

指标详情:
------------------------------------------------------------
  iou                           : 0.3034
  precision                     : 0.7234
  recall                        : 0.4521
  f1                            : 0.5567
  loss                          : 1.2650

============================================================
🎯 IoU指标
============================================================
  iou                           : 0.3034

✅ 评估完成!
```

## 注意事项

1. **必须激活环境**: 确保已激活 `mix_backup` 环境
2. **配置文件路径**: 使用训练时的完整YAML配置
3. **GPU内存**: 脚本会自动设置 batch_size=1
4. **训练进程**: 可以与训练同时运行（使用不同GPU或错开时间）

## 推荐使用场景

- ✅ **验证checkpoint**: 快速验证某个epoch的性能
- ✅ **对比不同epoch**: 批量评估多个checkpoint
- ✅ **调试训练**: 确认验证指标计算是否正确
- ✅ **最终评估**: 获取与训练时一致的性能报告
