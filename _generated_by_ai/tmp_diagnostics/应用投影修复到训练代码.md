# 应用投影修复到训练代码

## ✅ 问题已确认

测试结果显示修复有效：
- **边界内比例**: 59.5% → 100.0% (提升 40.5%)
- **Y 坐标范围**: 超出边界 → 完全在范围内
- **投影计算**: 正确

**根本原因**: `compute_mapping` 返回 `[y, x, valid]`，但代码当成了 `[x, y, valid]`

## 🔧 修复步骤

### 步骤 1: 修复 `dataset/data_loader.py`

找到第 326-329 行，修改如下：

```python
# 修复前 (错误)
x_label = single_mapping[:, 0][single_mapping[:, 0] != 0]
y_label = single_mapping[:, 1][single_mapping[:, 1] != 0]
x_label = torch.from_numpy(x_label).long()
y_label = torch.from_numpy(y_label).long()

# 修复后 (正确)
# 🔥 注意: compute_mapping 返回 [y, x, valid]，不是 [x, y, valid]
y_label = single_mapping[:, 0][single_mapping[:, 0] != 0]  # 第0列是 y
x_label = single_mapping[:, 1][single_mapping[:, 1] != 0]  # 第1列是 x
x_label = torch.from_numpy(x_label).long()
y_label = torch.from_numpy(y_label).long()
```

### 步骤 2: 添加运行时投影到 `dataset/open_vocab_dataset_v2.py`

修改 `_load_3d_simple` 函数，添加运行时投影计算：

```python
def _load_3d_simple(data_root: Path, split: str, scene_name: str,
                    data_2d_root: Optional[Path] = None) -> Dict[str, torch.Tensor]:
    """
    简单 3D 加载：从 data_root/split/scene_name.pth 读 (locs, feats, labels)。
    支持运行时计算投影标签。
    """
    pth_path = data_root / split / f"{scene_name}.pth"
    if not pth_path.exists():
        pth_path = data_root / split / f"{scene_name}_vh_clean_2.pth"
    if not pth_path.exists():
        # 占位数据...
        return {...}

    data = torch.load(pth_path, map_location="cpu", weights_only=False)
    if isinstance(data, (list, tuple)):
        locs, feats, labels = data[0], data[1], data[2]
    else:
        locs = data.get("locs", data.get("coords", torch.zeros(100, 3)))
        feats = data.get("feats", data.get("feat", torch.ones(locs.shape[0], 3)))
        labels = data.get("labels", torch.zeros(locs.shape[0], dtype=torch.long))

    if isinstance(locs, np.ndarray):
        locs = torch.from_numpy(locs)
    if isinstance(feats, np.ndarray):
        feats = torch.from_numpy(feats)
    if isinstance(labels, np.ndarray):
        labels = torch.from_numpy(labels)

    N = locs.shape[0]
    batch_idx = torch.zeros(N, 1, dtype=torch.long)
    coords_3d = torch.cat([batch_idx, locs.long()], dim=1)
    if feats.dim() == 1:
        feat_3d = feats.unsqueeze(1).expand(N, 3)
    else:
        feat_3d = feats.float()

    # 尝试从数据中获取 x_label 和 y_label
    x_label = data.get("x_label") if isinstance(data, dict) else None
    y_label = data.get("y_label") if isinstance(data, dict) else None
    
    if x_label is None or y_label is None:
        # 🔥 运行时计算投影
        if data_2d_root is not None and data_2d_root.exists():
            try:
                from utils.mapping_util import getMapping
                from glob import glob
                try:
                    from PIL import Image as PIL
                except ImportError:
                    PIL = None
                
                if PIL is None:
                    raise ImportError("需要 pillow 库来读取图像")
                
                scene_2d_dir = data_2d_root / scene_name
                img_dirs = sorted(glob(str(scene_2d_dir / "color" / "*")),
                                 key=lambda x: int(os.path.basename(x)[:-4]))
                
                if len(img_dirs) > 0:
                    # 选择中间帧 (或随机选一帧)
                    img_dir = img_dirs[len(img_dirs) // 2]
                    
                    # 读取位姿和深度
                    pose_path = img_dir.replace("color", "pose").replace(".jpg", ".txt")
                    depth_path = img_dir.replace("color", "depth").replace("jpg", "png")
                    
                    if os.path.exists(pose_path) and os.path.exists(depth_path):
                        pose = np.loadtxt(pose_path)
                        depth = np.array(PIL.open(depth_path)) / 1000.0
                        
                        # 计算投影
                        mapper = getMapping()
                        single_mapping = mapper.compute_mapping(pose, locs.numpy(), depth)
                        
                        # 🔥 关键: compute_mapping 返回 [y, x, valid]
                        # 提取有效投影
                        zero_rows = np.all(single_mapping != 0, axis=1)
                        valid_indices = np.where(zero_rows)[0]
                        
                        # 创建完整的 x_label 和 y_label
                        x_label = np.zeros(N, dtype=np.int64)
                        y_label = np.zeros(N, dtype=np.int64)
                        
                        # ✅ 正确: 第0列是 y，第1列是 x
                        y_label[valid_indices] = single_mapping[valid_indices, 0].astype(np.int64)
                        x_label[valid_indices] = single_mapping[valid_indices, 1].astype(np.int64)
                        
                        x_label = torch.from_numpy(x_label)
                        y_label = torch.from_numpy(y_label)
                        
                        num_valid = len(valid_indices)
                        print(f"Computed projection for {scene_name}: {num_valid}/{N} points ({num_valid/N*100:.1f}%)")
                    else:
                        raise FileNotFoundError(f"Pose or depth not found for {scene_name}")
                else:
                    raise FileNotFoundError(f"No images found for {scene_name}")
                    
            except Exception as e:
                print(f"Warning: Failed to compute projection for {scene_name}: {e}")
                x_label = torch.zeros(N, dtype=torch.long)
                y_label = torch.zeros(N, dtype=torch.long)
        else:
            print(f"Warning: No 2D data for {scene_name}, using zeros")
            x_label = torch.zeros(N, dtype=torch.long)
            y_label = torch.zeros(N, dtype=torch.long)
    else:
        # 已有投影标签，转换类型
        if isinstance(x_label, np.ndarray):
            x_label = torch.from_numpy(x_label)
        if isinstance(y_label, np.ndarray):
            y_label = torch.from_numpy(y_label)
        x_label = x_label.long()
        y_label = y_label.long()
        
        # 验证有效性
        if x_label.shape[0] != N or y_label.shape[0] != N:
            print(f"Warning: x_label/y_label shape mismatch in {pth_path}, using zeros")
            x_label = torch.zeros(N, dtype=torch.long)
            y_label = torch.zeros(N, dtype=torch.long)

    return {
        "coords_3d": coords_3d,
        "feat_3d": feat_3d,
        "ori_coords_3d": coords_3d.clone(),
        "inds_reconstruct": torch.arange(N, dtype=torch.long),
        "x_label": x_label,
        "y_label": y_label,
        "binary_label_3d": labels.long(),
        "binary_label_2d": torch.zeros(N, dtype=torch.long),
        "label_2d": torch.zeros(N, dtype=torch.long),
    }
```

### 步骤 3: 修改 `OpenVocabScannetDatasetV2` 传递 2D 数据路径

在 `__init__` 方法中：

```python
def __init__(self, config: OpenVocabDatasetV2Config):
    self.config = config
    self.precomputed_dir = Path(config.precomputed_dir) if config.precomputed_dir else None
    if not self.precomputed_dir or not self.precomputed_dir.exists():
        raise FileNotFoundError(f"precomputed_dir 不存在: {config.precomputed_dir}")

    # 获取 data_root 和 data_2d_root
    data_cfg = _load_yaml(config.data_config_path)
    data_root = data_cfg.get("DATA", {}).get("data_root", "")
    self.data_root = Path(data_root) if data_root else None
    
    # 🔥 新增: 获取 2D 数据路径
    data_2d_root = data_cfg.get("DATA", {}).get("data_root_2d", "")
    self.data_2d_root = Path(data_2d_root) if data_2d_root else None
    
    self.split = config.split
    # ... 其余代码
```

在 `__getitem__` 方法中：

```python
def __getitem__(self, idx: int) -> Dict[str, Any]:
    idx = idx % len(self.samples)
    scene_name, frame_stem = self.samples[idx]

    npz_path = self.precomputed_dir / scene_name / f"{frame_stem}_odise.npz"
    out = _load_npz_pooled(npz_path)

    if self.data_root and self.data_root.exists():
        # 🔥 传递 data_2d_root
        out_3d = _load_3d_simple(
            self.data_root, 
            self.split, 
            scene_name,
            data_2d_root=self.data_2d_root  # 传递 2D 数据路径
        )
    else:
        out_3d = _load_3d_simple(Path("/none"), self.split, scene_name)

    # ... 合并数据
    return out
```

## 📝 验证修复

### 1. 测试投影计算

```bash
cd /home/featurize/work/mix
python tmp_diagnostics/test_runtime_projection_fixed.py
```

应该看到：
```
✅ 边界内比例: 100.0%
✅ X 坐标在范围内
✅ Y 坐标在范围内
🎉 修复成功!
```

### 2. 测试训练

```bash
python train_open_vocab_v2.py \
    --config config/train_scannet_v2_minimal.yaml \
    --num-epochs 1
```

检查：
- ✅ Loss 不为 0
- ✅ 没有 "All x_label/y_label are 0" 警告
- ✅ 没有大量 "points out of bounds" 警告

## ⚡ 性能影响

根据测试结果：
- **单帧投影耗时**: ~0.1 秒
- **每个 epoch 总耗时**: ~1.7 分钟 (977 个场景)
- **10 个 epoch**: ~17 分钟

**结论**: 运行时计算投影的开销很小，完全可以接受！

## 📚 关键要点

1. **`compute_mapping` 返回顺序**: `[y, x, valid]`
   - `y` = 行索引 (height 维度)
   - `x` = 列索引 (width 维度)

2. **正确使用方式**:
   ```python
   mapping = mapper.compute_mapping(pose, coords, depth)
   y_label = mapping[:, 0]  # 第0列是 y
   x_label = mapping[:, 1]  # 第1列是 x
   ```

3. **为什么这样设计**: 
   - 符合 numpy 数组 `[row, col]` 索引习惯
   - OpenScene 原始实现

4. **访问 2D 数据时**:
   ```python
   # 正确
   mask_value = mask_2d[y_idx, x_idx]  # [row, col]
   
   # 或对于批量数据
   mask_values = mask_2d[:, y_idx, x_idx]  # (K, H, W) 格式
   ```

## 🎯 总结

- ✅ 问题: 坐标顺序颠倒
- ✅ 原因: `compute_mapping` 返回 `[y, x, valid]` 被当成 `[x, y, valid]`
- ✅ 修复: 交换 `x_label` 和 `y_label` 的赋值
- ✅ 验证: 边界内比例从 59.5% 提升到 100%
- ✅ 性能: 运行时计算开销小 (~1.7 分钟/epoch)

**现在可以正常训练了！** 🎉
