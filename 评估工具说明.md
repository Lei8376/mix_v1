# Mix 模型评估工具 - 完整方案

## 📋 概述

我已经为你创建了完整的评估工具，用于测试 Mix 项目训练的开放词汇（Open Vocabulary）3D 分割模型。该工具参考了 OpenScene 的评估方式，可以输出：

- ✅ **总体 mIoU**（Mean Intersection over Union）
- ✅ **各个类别的 IoU**（wall, floor, cabinet, chair 等 20 个类别）
- ✅ **详细的评估报告**

## 📁 创建的文件

### 1. **eval_mix_model.py** - 主评估脚本
完整的评估脚本，支持：
- 加载训练权重
- 使用 CLIP 提取文本特征（开放词汇关键技术）
- 在验证集上计算 mIoU
- 输出详细的每类 IoU

### 2. **EVAL_README.md** - 详细使用文档
包含：
- 完整的使用说明
- 参数说明
- 示例命令
- 常见问题解答
- 输出格式说明

### 3. **test_eval_env.sh** - 环境测试脚本
快速检查环境是否配置正确

### 4. **test_checkpoint.py** - 简化测试脚本
仅测试权重文件能否正确加载

## 🚀 快速开始

### 第一步：激活环境

```bash
source /home/featurize/work/envs/mix_backup/bin/activate
cd /home/featurize/work/mix
```

### 第二步：测试环境

```bash
./test_eval_env.sh
```

如果看到所有项都是 ✓，说明环境配置正确。

### 第三步：运行评估

```bash
# 评估 epoch 19 的权重
python eval_mix_model.py --checkpoint checkpoints/full.1/checkpoint_epoch_19.pth

# 评估 epoch 10 的权重
python eval_mix_model.py --checkpoint checkpoints/full.1/checkpoint_epoch_10.pth

# 评估 epoch 22 的权重（如果存在）
python eval_mix_model.py --checkpoint checkpoints/full.4/checkpoint_epoch_22.pth
```

## 📊 输出示例

```
============================================================
评估轮次 1 结果:
============================================================

evaluating 15234567 points...
classes          IoU
----------------------------
wall          : 0.756   (123456/234567)
floor         : 0.823   (234567/345678)  ← 这就是 floor 的 mIoU
cabinet       : 0.654   (  3456/  5678)
bed           : 0.712   ( 12345/ 17890)
chair         : 0.689   ( 23456/ 34567)
sofa          : 0.645   ( 12345/ 19123)
table         : 0.678   ( 23456/ 34567)
door          : 0.701   ( 12345/ 17612)
window        : 0.634   (  8901/ 14023)
bookshelf     : 0.612   (  4567/  7456)
picture       : 0.523   (  2345/  4482)
counter       : 0.598   (  3456/  5779)
desk          : 0.623   (  5678/  9113)
curtain       : 0.587   (  3456/  5886)
refrigerator  : 0.701   (  2345/  3345)
shower curtain: 0.634   (  1234/  1947)
toilet        : 0.789   (  4567/  5788)
sink          : 0.723   (  3456/  4780)
bathtub       : 0.745   (  2345/  3149)
other         : 0.456   ( 12345/ 27079)
Mean IoU 0.6534          ← 这就是总体 mIoU
Mean Acc 0.7312
```

## 🔍 关键技术点

### 开放词汇（Open Vocabulary）评估

与传统的固定类别分割不同，开放词汇模型的评估包含以下关键步骤：

1. **文本特征提取**
   - 使用 CLIP 将类别名称（如 "wall", "floor"）编码为特征向量
   - 这使得模型可以识别训练时未见过的类别

2. **3D 特征提取**
   - 使用 MinkowskiNet 等 3D backbone 提取点云特征

3. **特征匹配**
   - 计算 3D 特征与文本特征的相似度（通过点积）
   - 为每个 3D 点分配最相似的类别标签

4. **IoU 计算**
   - 使用 OpenScene 的 metric 模块
   - 计算每个类别的 IoU = TP / (TP + FP + FN)
   - 平均所有类别得到 mIoU

## 📈 评估多个 Checkpoint

如果你想知道哪个 epoch 的模型效果最好：

```bash
# 创建一个批量评估脚本
cat > eval_all_checkpoints.sh << 'EOF'
#!/bin/bash
source /home/featurize/work/envs/mix_backup/bin/activate
cd /home/featurize/work/mix

echo "开始批量评估..."
echo ""

for ckpt in checkpoints/full.1/checkpoint_epoch_*.pth; do
    echo "=========================================="
    echo "评估: $ckpt"
    echo "=========================================="
    python eval_mix_model.py --checkpoint "$ckpt" | grep "Mean IoU"
    echo ""
done
EOF

chmod +x eval_all_checkpoints.sh
./eval_all_checkpoints.sh
```

## 🛠️ 高级用法

### 多次重复评估（减少随机性）

MinkowskiNet 的体素化有随机性，可以多次评估取平均：

```bash
python eval_mix_model.py \
    --checkpoint checkpoints/full.1/checkpoint_epoch_19.pth \
    --test-repeats 3
```

### 指定数据集路径

如果数据集不在默认位置：

```bash
python eval_mix_model.py \
    --checkpoint checkpoints/full.1/checkpoint_epoch_19.pth \
    --data-root /your/path/to/scannet_3d \
    --precomputed-dir /your/path/to/pixel_pooled
```

### 使用不同的 3D Backbone

如果训练时使用了不同的 backbone：

```bash
python eval_mix_model.py \
    --checkpoint checkpoints/full.1/checkpoint_epoch_19.pth \
    --pc-arch MinkUNet34C  # 或 MinkUNet18A
```

## ⚠️ 常见问题

### Q: 显示 "找不到模块"
**A:** 确保已激活环境：
```bash
source /home/featurize/work/envs/mix_backup/bin/activate
```

### Q: CUDA out of memory
**A:** 减小 batch size：
```bash
python eval_mix_model.py --checkpoint xxx.pth --test-batch-size 1
```

### Q: 找不到数据集
**A:** 检查数据集路径：
```bash
ls /home/featurize/data/scannet_3d/val/
```
或使用 `--data-root` 参数指定正确路径。

### Q: 评估很慢
**A:** 
1. 确保使用了预计算特征（`--precomputed-dir`）
2. 增加 batch size（如果显存允许）
3. 减少 num_workers 避免 I/O 瓶颈

## 📝 与训练对比

训练时的验证 mIoU 记录在：
- TensorBoard 日志: `runs/full.1/`
- Checkpoint 中: `checkpoint['val_miou']`

可以用 TensorBoard 查看训练曲线：
```bash
tensorboard --logdir runs/
```

然后在浏览器打开 `http://localhost:6006`

## 🎯 预期结果

根据你提到的 "mIoU 过低" 问题，评估后可以：

1. **确认当前性能**：看看各个 epoch 的 mIoU 具体是多少
2. **找出问题类别**：哪些类别的 IoU 特别低（如 floor）
3. **对比训练日志**：评估 mIoU 与训练日志是否一致
4. **分析原因**：
   - 是否是某些类别数据不足？
   - 是否是模型过拟合/欠拟合？
   - 是否是数据增强或超参数问题？

## 📞 下一步

运行评估后，如果发现问题，可以：

1. **调整训练参数**：学习率、权重衰减、数据增强等
2. **修改模型架构**：更大的 backbone、不同的融合方式等
3. **数据分析**：检查哪些类别表现差，是否需要更多数据
4. **对比 OpenScene**：看看 OpenScene 在相同数据上的表现如何

---

如有任何问题，请参考 `EVAL_README.md` 或检查脚本注释。

祝评估顺利！🎉
